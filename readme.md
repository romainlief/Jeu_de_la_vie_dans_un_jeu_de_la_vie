# Game of Life in a Game of Life

To run the Game of Life in a game of life simulation, navigate to the `src` directory and execute the main script using Python 3:

```bash
cd src && python3 -m main
```
